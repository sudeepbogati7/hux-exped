# Git LFS removed — all assets are committed as normal Git objects so they
# deploy reliably on Vercel (no LFS bandwidth/pointer issues).
# Keep binary assets out of line-ending normalization.
*.png  -text
*.jpg  -text
*.jpeg -text
*.JPG  -text
*.webp -text
*.mp4  -text
*.ico  -text
